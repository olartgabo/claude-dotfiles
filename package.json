{
  "name": "claude-dotfiles",
  "version": "1.0.0",
  "private": true,
  "description": "Shared Claude Code configuration and guard hooks for the MCPJam team",
  "type": "module",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "test": "node tests/guards.test.mjs",
    "install:config": "node install.mjs",
    "install:config:dry": "node install.mjs --dry-run"
  }
}
